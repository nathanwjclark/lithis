# deps / build
node_modules/
dist/
*.tsbuildinfo

# env & secrets — never commit credentials
.env
.env.*
!.env.example

# local data
data/
*.local.json

# OS noise
.DS_Store

# stub census artifacts (regenerated by CI)
stub-census.json

# local blob storage (context module default LITHIS_BLOB_DIR)
var/
