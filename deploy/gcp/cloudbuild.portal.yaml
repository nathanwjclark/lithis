# Cloud Build config for the lithis portal image (context = repo root).
steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-f", "deploy/docker/Dockerfile.portal", "-t", "${_IMAGE}", "."]
images:
  - "${_IMAGE}"
substitutions:
  _IMAGE: "" # supplied by 50-build-images.sh
options:
  logging: CLOUD_LOGGING_ONLY
